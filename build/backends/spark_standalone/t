#!/bin/bash

SPARK_VERSION=1.5.0

#echo $SPARK_VERSION | grep "^1.[123].[0-9]" > /dev/null
tmp=`echo $SPARK_VERSION | grep "^1.[123].[0-9]"`
if [ $? -eq 0 ]; then   # spark 1.3 or prior
#if [[ $SPARK_VERSION == "^1.[123].[0-9]" ]]; then   # spark 1.3 or prior
	echo "1.3 or prior"
else
	echo "spark://`hostname`:$SPARK_MASTER_PORT"
fi
